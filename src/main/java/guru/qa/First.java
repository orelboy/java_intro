package guru.qa;

public class First {
    //psvm для быстрого создания метода
    public static void main(String[] args) {
       // String str = "Danila"; // это объект класа строка
        Person teacher = new Person( 33, "Данил"); // это объект класа Person
    //    teacher.age = 66;
    //    teacher.name = "Danila";
        teacher.goToPension();
        teacher.sayName();











 /*       System.out.println("Привет, мир!");

        boolean flag = true;

        byte aByte = 0; // 8 бит -128 (2^ (8-1))---127
        short aShort = 0; // 16
        int aInt = 0; // 32
        long aLong = 0L; // 64

        float aFloat = 0.0F; // 32
        double aDouble = 0.0; // 64

        char aChar = 'a';
        String Name = "Danila";

        // маетематические операторы деления "/"-целочисленое деление (5/2=2); "%"- остаток от деления (5%2=1)

        // сравнение == ; <= ; >= ; !=

        // оператор присваивания = ;  i += j это тоже самое что  (i = i + j)

        // Логические операторы ! - не; && - и; || =  или;

        // Оператор instanceof для классов ператор instanceof нужен, чтобы проверить, был ли объект, на который ссылается переменная X, создан на основе какого-либо класса Y.

        // Тернарный операторЗамена конструкции if-else    String securityAnswer = man.getAge() >= 18 ? "Все в порядке, проходите!" : "Этот фильм не подходит для вашего возраста!";

*/



    }



}
